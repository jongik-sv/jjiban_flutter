# 공통 모듈: 자동 Git Commit

> 이 파일은 `/wf:*` 명령어에서 파일 생성/수정 후 자동 커밋을 수행합니다.

---

## 자동 커밋 규칙

### 1. 커밋 시점

| 명령어 | 커밋 시점 | 커밋 대상 |
|--------|----------|----------|
| `/wf:start` | 기본설계 문서 생성 후 | `010-*.md`, `wbs.md` |
| `/wf:ui` | 화면설계 문서 생성 후 | `011-ui-design.md`, `ui-assets/` |
| `/wf:draft` | 상세설계 문서 생성 후 | `020-*.md`, `025-*.md`, `026-*.md`, `wbs.md` |
| `/wf:build` | 구현 완료 후 | 소스 코드, `030-*.md`, `wbs.md` |
| `/wf:test` | 테스트 결과 생성 후 | `070-*.md`, `test-results/` |
| `/wf:audit` | 리뷰 문서 생성 후 | `021-*.md`, `031-*.md` |
| `/wf:patch` | 코드 수정 후 | 수정된 소스 코드 |
| `/wf:verify` | 통합테스트 완료 후 | `070-integration-test.md`, `wbs.md` |
| `/wf:done` | 매뉴얼 생성 후 | `080-manual.md`, `wbs.md` |
| `/wf:fix` | 결함 수정 후 | 수정된 소스 코드, `wbs.md` |

### 2. 커밋 메시지 형식

```
[{command}] {Task-ID}: {summary}

- {변경 내용 1}
- {변경 내용 2}
...

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### 3. 커밋 메시지 예시

| 명령어 | 예시 메시지 |
|--------|------------|
| `/wf:start` | `[wf:start] TSK-01-01-01: 기본설계 완료` |
| `/wf:ui` | `[wf:ui] TSK-01-01-01: 화면설계 완료` |
| `/wf:draft` | `[wf:draft] TSK-01-01-01: 상세설계 완료` |
| `/wf:build` | `[wf:build] TSK-01-01-01: 구현 완료` |
| `/wf:test` | `[wf:test] TSK-01-01-01: 테스트 결과 저장` |
| `/wf:audit` | `[wf:audit] TSK-01-01-01: LLM 코드 리뷰 완료` |
| `/wf:patch` | `[wf:patch] TSK-01-01-01: 리뷰 반영 완료` |
| `/wf:verify` | `[wf:verify] TSK-01-01-01: 통합테스트 완료` |
| `/wf:done` | `[wf:done] TSK-01-01-01: 작업 완료 및 매뉴얼 생성` |
| `/wf:fix` | `[wf:fix] TSK-01-01-01: 결함 수정 완료` |

---

## 자동 실행 단계 (명령어 마지막에 실행)

### 커밋 실행 절차

1. **변경 파일 확인**:
   ```bash
   git status --short
   ```

2. **변경된 파일이 있는 경우에만 커밋**:
   - 생성/수정된 파일이 없으면 커밋 스킵
   - 변경 파일이 있으면 아래 진행

3. **파일 스테이징**:
   ```bash
   git add .jjiban/projects/{project}/tasks/{TSK-ID}/
   git add .jjiban/projects/{project}/wbs.md
   # 소스 코드 변경 시 (wf:build, wf:patch, wf:fix)
   git add app/ server/ tests/
   ```

4. **커밋 실행**:
   ```bash
   git commit -m "[{command}] {Task-ID}: {summary}

   - {변경 내용 목록}

   🤖 Generated with [Claude Code](https://claude.com/claude-code)

   Co-Authored-By: Claude <noreply@anthropic.com>"
   ```

5. **커밋 결과 출력**:
   ```
   📦 Git Commit 완료:
   ├── 커밋 해시: abc1234
   ├── 메시지: [wf:build] TSK-01-01-01: 구현 완료
   └── 변경 파일: 5개
   ```

---

## 커밋 스킵 조건

| 조건 | 동작 |
|------|------|
| 변경 파일 없음 | 커밋 스킵, 메시지 출력 |
| git 저장소 아님 | 커밋 스킵, 경고 출력 |
| 커밋 실패 | 에러 출력, 명령어는 성공 처리 |

---

## 출력 예시

### 정상 커밋
```
📦 Git Commit 완료:
├── 커밋 해시: abc1234
├── 메시지: [wf:build] TSK-01-01-01: 구현 완료
├── 변경 파일: 5개
│   ├── .jjiban/projects/jjiban/tasks/TSK-01-01-01/030-implementation.md
│   ├── .jjiban/projects/jjiban/wbs.md
│   ├── app/pages/project.vue
│   ├── server/api/projects/index.ts
│   └── tests/api/projects.test.ts
└── 상태: ✅ 성공
```

### 변경 없음
```
📦 Git Commit 스킵:
└── 이유: 변경된 파일 없음
```

### 커밋 실패
```
📦 Git Commit 실패:
├── 에러: pre-commit hook failed
└── 상태: ⚠️ 명령어 실행은 완료됨 (수동 커밋 필요)
```

---

<!--
jjiban 프로젝트 - Workflow Auto Commit Module
Version: 1.0
author: Claude

Changes (v1.0):
- 자동 커밋 기능 추가
- 커밋 메시지 형식 정의
- 커밋 스킵 조건 정의
-->
