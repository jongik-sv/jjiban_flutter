---
name: design:detail
description: "상세설계 및 UI 테스트케이스 생성 (최적화 버전)"
category: design
complexity: moderate
wave-enabled: false
performance-profile: optimized
auto-flags:
  - --seq
  - --c7
  - --token-efficient
mcp-servers: [sequential, context7]
personas: [architect, scribe]
---

# /design:detail - 상세설계 및 UI 테스트케이스 생성 (v1)

> **최적화된 설계 전문화**: Task의 요구사항을 바탕으로 상세설계서와 UI 중심 테스트케이스를 효율적으로 생성합니다.

## 트리거
- Task의 상세설계서가 필요한 경우
- UI 중심의 매뉴얼 테스트케이스 작성이 필요한 경우
- 요구사항을 바탕으로 구현 가능한 수준의 설계가 필요한 경우

## 사용법
```bash
# Task 번호로 실행 (권장)
/design:detail Task 3.1
/design:detail 3.1

# 기존 방식도 지원
/design:detail "Task-3-1.MR0100-Backend-API-구현"
```

## 자동 실행 플로우

### 1단계: Task 정보 수집 및 분석
**Auto-Persona**: architect
**MCP**: sequential + context7

**자동 실행 단계**:
1. **Task 정보 추출 및 파싱**:
   ```javascript
   // "/design:detail Task 3.1" 또는 "/design:detail 3.1"에서 Task 번호 추출
   function parseTaskFromCommand(input) {
       const taskPattern = /(?:Task\s+)?(\d+\.\d+)/i;
       return input.match(taskPattern)?.[1]; // "3.1" 추출
   }
   ```

2. **공통 문서 분석 함수 호출**: `analyze_project_context(task_number)`
   - 📋 함수 참조: `@docs/common/07.functions/analyze_project_context.md`
   - `./docs/project/maru/00.foundation/01.project-charter/tasks.md`에서 Task 정보 조회
   - 만약 UI 설계일 경우 해당하는 task의 Backend 상세 설계 문서를 참조할 것.
   - 자동 파일명 생성: `{task-id}.{task명}(상세설계).md`
     - 예: `Task-3-1.MR0100-Backend-API-구현(상세설계).md`
   - 프로젝트 정보 및 Task 상세 정보 수집
   - 관련 참조 문서 자동 수집 (PRD, 기본 설계, 기술 명세)

3. **설계 컨텍스트 구성**: 분석된 정보를 기반으로 상세설계 준비

### 2단계: 상세설계서 작성
**Auto-Persona**: architect + scribe
**MCP**: sequential + context7

**자동 실행 단계**:
1. 상세설계 템플릿 로드 (`./docs/common/04.template/detail_design_template.md`)
2. 요구사항 분석 및 기능 정의
  - 요구사항의 요구사항 ID가 없다면 `[REQ-001]` 형태로 요구사항 생성
3. 시스템 아키텍처 설계
4. UI/UX 설계 및 데이터 인터페이스 설계
  - UI/UX 설계 시 화면 설계에 대해 SVG 파일 생성
5. 보안 고려사항 및 성능 요구사항 정의
6. 요구사항 대비 설계 완전성 검증
7. 기본 설계 대비 상세설계 일관성 검증
8. **Task 상태 업데이트**: `update_task_status(task_number, "detail_design")`
   - 📋 함수 참조: `@docs/common/07.functions/update_task_status.md`
   - tasks.md에서 Task 체크박스를 [d]로 변경
   - 진행 상태를 **[d]**로 표시

### 3단계: UI 중심 테스트케이스 생성
**Auto-Persona**: quality-engineer
**MCP**: sequential

**자동 실행 단계**:
1. **UI 컴포넌트 기반 테스트케이스 설계**:
   - 화면 요소별 동작 테스트
   - 사용자 시나리오 기반 흐름 테스트
   - 반응형 및 접근성 테스트케이스
2. **매뉴얼 테스트케이스 작성**:
   - 사람이 직접 실행 가능한 테스트 단계
   - 예상 결과 및 검증 기준 명시
   - 우선순위 및 중요도 분류
3. **UI 자산 및 테스트 데이터 준비**:
   - 화면 설계 SVG 파일 생성
   - 테스트용 샘플 데이터 정의

**산출물**: 상세설계서 내 "UI 테스트케이스" 섹션

## 🎯 최적화 특징

### ✅ 토큰 효율성 (50% 절약)
- 플래그 제거로 조건부 로직 단순화
- 공통 함수로 중복 분석 제거
- UI 중심 테스트케이스로 실용성 향상

### 🎨 UI 중심 설계
- 사용자 경험 우선 설계 접근
- 시각적 자산 자동 생성 (SVG)
- 실제 실행 가능한 테스트케이스

### 📊 품질 보증
- 템플릿 기반 일관된 구조
- 다층 검증 (완전성, 일관성)
- 요구사항 추적성 확보

## 산출물 위치

### SDD v1 폴더 구조 기준
- **기본설계서**: `./docs/project/[Project]/10.design/11.basic-design/{task-id}.{Task명}(기본설계).md`
- **상세설계서**: `./docs/project/[Project]/10.design/12.detail-design/{task-id}.{Task명}(상세설계).md`
- **UI 자산**: `./docs/project/[Project]/10.design/12.detail-design/ui-assets/{task-id}.{task명}/`
- **추적성 매트릭스**: `./docs/project/[Project]/10.design/12.detail-design/{task-id}.{task명}(traceability).csv`

**예시**:
- `./docs/project/maru/10.design/11.basic-design/Task-1.개발환경구성-및-기반설정(기본설계).md`
- `./docs/project/maru/10.design/12.detail-design/Task-3-1.MR0100-Backend-API-구현(상세설계).md`
- `./docs/project/maru/10.design/12.detail-design/Task-3-2.MR0100-Frontend-UI-구현(상세설계).md`

---

**다음 명령어**: `/dev:implement` - TDD 기반 구현 수행